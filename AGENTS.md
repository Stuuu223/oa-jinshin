# 金石管理系统(oa-agent/jinshi)——项目规则

## 问题排查 SOP(harness,蒸馏自 `jinshi-system/29-排查SOP-harness.md`)

- **98% 的问题根因在代码**——排查先查代码(git log 变化点窗口/读相关代码),不猜缓存/服务/环境/版本(那是 2%,且 28 号复盘全部被打脸)
- 代码层排查顺序:最近改动 → 事件绑定/遮罩残留/组件状态 → 定时器/循环/内存 → 资源加载/404/MIME
- 证据优先:用户贴的浏览器证据(HTML/Network/console/调试输出)直接命中根因——要证据再下结论(可视化调试:console.warn+页面横幅断点定位)
- 禁止:问题解决不了就猜"缓存/服务/环境/版本";让用户/职员做环境操作(清缓存/重置/换浏览器)作为首要手段;证据不足下结论;打补丁(绕过组件状态=屎山)
- 代码层全排除后才考虑 2% 环境因素;多因素根因(一个症状可能是多个代码问题叠加——系统性查)
